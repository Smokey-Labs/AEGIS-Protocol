# Change Envelope

A Change Envelope is a structured summary of system drift.

It must include:
- What changed
- Why it matters
- What remains unknown
- Whether human review is required

No system may emit raw deltas without envelope context.
